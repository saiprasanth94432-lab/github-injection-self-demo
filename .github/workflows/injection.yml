name: Injection-Demo

on:
    issues:
        types: [opened]
jobs: 
    test:
        runs-on: ubuntu-latest
        steps:
            - name: Print Issue title
              run: |
                 title=${{github.event.issue.title}}
                 echo $title